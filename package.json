{
	"name": "parcel_editor",
	"version": "7.0.1",
	"description": "Editor for the parcel game",
	"source": "source/index.html",
	"scripts": {
		"start": "rm -rf docs && mkdir -p docs && parcel --dist-dir docs",
		"build": "rm -rf docs && mkdir -p docs && parcel build --dist-dir docs --public-url /Parcel-Editor/ && touch docs/.nojekyll"
	},
	"author": "Kai Wright",
	"license": "MIT",
	"private": true,
	"packageManager": "yarn@4.1.0",
	"devDependencies": {
		"@parcel/transformer-sass": "2.12.0",
		"@typescript-eslint/eslint-plugin": "^7.2.0",
		"@typescript-eslint/parser": "^7.2.0",
		"eslint": "^8.57.0",
		"parcel": "^2.12.0"
	}
}
